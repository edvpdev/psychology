import React from 'react'

import styles from './footer.module.scss'

const Footer = () => {
  return <div className={`${styles.footer} ${styles.bgLight}`}>&copy; Copyrights</div>
}

export default Footer
